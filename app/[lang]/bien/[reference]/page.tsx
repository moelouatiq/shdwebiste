import {notFound} from "next/navigation";import {Header} from "@/components/Header";import {Footer} from "@/components/Footer";import {ContactForm} from "@/components/ContactForm";import {getDb} from "@/lib/mongodb";import {localizeProperty} from "@/lib/properties";import type {Lang,Property} from "@/lib/types";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{lang:Lang;reference:string}>}){const {lang,reference}=await params;let row;try{row=await (await getDb()).collection<Property>("properties").findOne({reference:decodeURIComponent(reference),published:true});}catch{}if(!row)notFound();const {_id,...plain}=row;const p=localizeProperty(plain,lang);return <><Header lang={lang}/><main className="detail wrap"><div className="detailGallery">{p.images.length?p.images.map((x,i)=><img key={x} src={x} alt={`${p.title} ${i+1}`}/>):<div className="imageFallback"/>}</div><div className="detailInfo"><small>{p.reference} · {p.type}</small><h1>{p.title}</h1><p className="location">⌖ {p.address}, {p.district}, {p.city}</p><strong className="detailPrice">{p.price.toLocaleString()} {p.currency}</strong><div className="facts"><span>▣ {p.bedrooms}</span><span>♨ {p.bathrooms}</span><span>▱ {p.surface} m²</span></div><p>{p.description}</p><div className="amenities">{p.amenities.map(x=><span key={x}>✓ {x}</span>)}</div><h2>Contact</h2><ContactForm lang={lang} propertyReference={p.reference}/></div></main><Footer lang={lang}/></>}
