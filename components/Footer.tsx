import Link from "next/link";import type {Lang} from "@/lib/types";import {t} from "@/lib/i18n";
export function Footer({lang}:{lang:Lang}){const c=t(lang);return <><section className="newsletter"><div className="wrap newsletterInner"><div><small>RESTEZ INFORMÉ</small><h3>{c.newsletter}</h3></div><form><input type="email" placeholder={c.email}/><button className="btn" type="button">{c.subscribe}</button></form></div></section><footer><div className="wrap footerInner"><div className="brand"><span className="brandIcon">⌂</span><span><b>SHD Immobilier</b><small>Votre patrimoine, notre priorité</small></span></div><div className="footerLinks"><Link href={`/${lang}`}>{c.navHome}</Link><Link href={`/${lang}/biens`}>{c.navProperties}</Link><Link href={`/${lang}#contact`}>{c.navContact}</Link></div><small>© {new Date().getFullYear()} SHD Immobilier. Tous droits réservés.</small></div></footer></>}
