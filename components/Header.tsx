"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {useState} from "react";import type {Lang} from "@/lib/types";import {t} from "@/lib/i18n";
export function Header({lang}:{lang:Lang}){const c=t(lang),path=usePathname(),[open,setOpen]=useState(false);const switchPath=(l:Lang)=>path.replace(/^\/(fr|en|ar)/,`/${l}`);return <header className="header"><div className="nav wrap"><Link className="brand" href={`/${lang}`}><span className="brandIcon">⌂</span><span><b>SHD Immobilier</b><small>{lang==="ar"?"ممتلكاتك، أولويتنا":lang==="en"?"Your property, our priority":"Votre patrimoine, notre priorité"}</small></span></Link><button className="menu" onClick={()=>setOpen(!open)} aria-label="Menu">☰</button><nav className={open?"navLinks open":"navLinks"}><Link href={`/${lang}`}>{c.navHome}</Link><Link href={`/${lang}/biens`}>{c.navProperties}</Link><Link href={`/${lang}#proprietaire`}>{c.navOwner}</Link><Link href={`/${lang}#apropos`}>{c.navAbout}</Link><Link href={`/${lang}#contact`}>{c.navContact}</Link><div className="langs">{(["fr","en","ar"] as Lang[]).map(l=><Link key={l} className={l===lang?"active":""} href={switchPath(l)}>{l.toUpperCase()}</Link>)}</div><Link className="btn small" href={`/${lang}#contact`}>{c.contactUs}</Link></nav></div></header>}
