"use client";import {useEffect,useMemo,useState} from "react";import {Header} from "./Header";import {Footer} from "./Footer";import {PropertyCard} from "./PropertyCard";import type {Lang,PublicProperty} from "@/lib/types";import {t} from "@/lib/i18n";
export function PropertiesPage({lang}:{lang:Lang}){const c=t(lang),[items,setItems]=useState<PublicProperty[]>([]),[loading,setLoading]=useState(true),[city,setCity]=useState(""),[type,setType]=useState(""),[max,setMax]=useState("");useEffect(()=>{fetch(`/api/properties?lang=${lang}`).then(r=>r.json()).then(d=>setItems(d.properties||[])).finally(()=>setLoading(false))},[lang]);const cities=useMemo(()=>[...new Set(items.flatMap(p=>[p.city,p.district]).filter(Boolean))],[items]);const types=useMemo(()=>[...new Set(items.map(p=>p.type).filter(Boolean))],[items]);const filtered=items.filter(p=>(!city||p.city===city||p.district===city)&&(!type||p.type===type)&&(!max||p.price<=Number(max)));return <><Header lang={lang}/><main><section className="listingHero"><div className="wrap"><small>SHD IMMOBILIER</small><h1>{c.navProperties}</h1><p>{c.featuredText}</p></div></section><section className="listing wrap"><div className="filterBox"><h2>{c.filters}</h2><select value={city} onChange={e=>setCity(e.target.value)}><option value="">{c.city}</option>{cities.map(x=><option key={x}>{x}</option>)}</select><select value={type} onChange={e=>setType(e.target.value)}><option value="">{c.type}</option>{types.map(x=><option key={x}>{x}</option>)}</select><input type="number" value={max} onChange={e=>setMax(e.target.value)} placeholder={c.budget}/></div>{loading?<p>{c.loading}</p>:filtered.length?<div className="propertyGrid">{filtered.map(p=><PropertyCard key={p.reference} property={p} lang={lang}/>)}</div>:<div className="empty">{c.empty}</div>}</section></main><Footer lang={lang}/></>}
