import type {PublicProperty} from "@/lib/types";import Link from "next/link";import type {Lang} from "@/lib/types";import {t} from "@/lib/i18n";
export function PropertyCard({property:p,lang}:{property:PublicProperty;lang:Lang}){const image=p.images?.[0]||"https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=1200&q=80";return <article className="propertyCard"><div className="propertyImage" style={{backgroundImage:`url(${image})`}}><span>{p.type}</span></div><div className="propertyBody"><h3>{p.title}</h3><p className="location">⌖ {p.district?p.district+", ":""}{p.city}</p><div className="facts"><span>▣ {p.bedrooms}</span><span>♨ {p.bathrooms}</span><span>▱ {p.surface} m²</span></div><div className="cardBottom"><strong>{new Intl.NumberFormat(lang==="ar"?"ar-MA":lang==="en"?"en-US":"fr-FR").format(p.price)} {p.currency}</strong><Link href={`/${lang}/bien/${encodeURIComponent(p.reference)}`}>{t(lang).details} →</Link></div></div></article>}
